# Fallback if Coolify uses Nixpacks instead of Dockerfile.
# Prefer: Build Pack → Dockerfile in Coolify settings.

[variables]
NIXPACKS_NODE_VERSION = "22"

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
